 $(document).ready(function(){ 
 
	$('#started_date').datepicker({
      format: 'dd-mm-yyyy',
      language: 'id' // Use 'id' for Indonesian language if needed
    });

	$('#ended_date').datepicker({
      format: 'dd-mm-yyyy',
      language: 'id' // Use 'id' for Indonesian language if needed
    });	
 
 });